commit-msg:
  commands:
    lint-commit-message:
      run: bunx commitlint --edit {1}

pre-commit:
  commands:
    lint:
      glob: "*.{js,ts,jsx,tsx}"
      run: bunx biome check --apply {staged_files}